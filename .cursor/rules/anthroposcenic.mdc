---
description: Anthroposcenic project context, stack, and pipeline
alwaysApply: true
---

# Anthroposcenic

Local image pipeline: upload → Ollama describe → configure ComfyUI → render → archive.

## Stack

| Layer | Technology |
|-------|------------|
| App | Next.js 14 App Router, React 18, TypeScript |
| UI | Tailwind CSS v4, ShadCN v4 (Radix Nova), Geist, Sonner |
| Vision | Ollama — `anthroposcenic-describe:latest` (from `llava:7b`) |
| Generation | ComfyUI — Flux GGUF default, SD checkpoints optional |
| Progress | ComfyUI WebSocket + HTTP poll; SSE to browser |

`npm run dev` starts Ollama + Next.js only. ComfyUI starts on demand during `/process`.

## UI routes

`/` → `/upload` → `/describe` → `/configure` → `/process` → `/complete` · `/archive`

Pipeline state: `sessionStorage` via `lib/pipeline-storage.ts`. `imageId` in URL between steps. Archive blend uses `imageIds` → txt2img `mode=blend`.

## Key paths

- `app/api/` — route handlers (describe SSE, comfyui process SSE)
- `lib/comfyui.ts` — programmatic workflows (Flux `UnetLoaderGGUF`, SD `CheckpointLoaderSimple`)
- `lib/comfyui-process-stream.ts` — shared process subscription + recovery poll
- `lib/output-archive.ts` — archive listing from `comfyui/output/`
- `types/index.ts` — shared API/types
- `docs/` — architecture, API, configuration (prefer over stale comments)

## Scope

- No separate transform step; config is manual in the configure UI.
- Minimize diff scope; match existing patterns in surrounding code.
- Do not add tests unless requested.
